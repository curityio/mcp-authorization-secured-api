_format_version: '2.1'
_transform: true

services:

- name: librechat-api
  url: http://librechat-api:3080
  routes:
  - name: librechat-api-route
    hosts:
    - agent.external.example
    paths:
    - /

- name: utility-mcp-server-well-known
  url: http://utility-mcp-server:3000/.well-known
  routes:
  - name: utility-mcp-server-well-known-resource-route
    hosts:
    - mcp.demo.example
    paths:
    - /.well-known

- name: utility-mcp-server
  url: http://utility-mcp-server:3000
  routes:
  - name: utility-mcp-server-route
    hosts:
    - mcp.demo.example
    paths:
    - /
  plugins:
    - name: phantom-token
      config:
        introspection_endpoint: http://idsvr:8443/oauth/v2/oauth-introspect
        client_id: api-gateway-client
        client_secret: Password1
        token_cache_seconds: 900

- name: stocks-api
  url: http://stocks-api:3000/stocks
  routes:
  - name: stocks-api-route
    hosts:
    - api.demo.example
    paths:
    - /stocks

- name: admin-ui
  url: http://idsvr:6749
  routes:
  - name: admin-ui-route
    hosts:
    - admin.demo.example
    paths:
    - /

- name: authorization-server-metadata
  url: http://idsvr:8443/.well-known/oauth-authorization-server/oauth/v2/oauth-anonymous
  routes:
  - name: oauth-metadata-route
    hosts:
    - login.demo.example
    paths:
    - /.well-known/oauth-authorization-server

- name: authorization-server
  url: http://idsvr:8443
  routes:
  - name: oauth-route
    hosts:
    - login.demo.example
    paths:
    - /
