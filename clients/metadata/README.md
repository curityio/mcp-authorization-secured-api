# Client ID Metadata Document Endpoint

This simple node server hosts the Client ID Metadata Document for some example clients in this repository. It's used to demonstrate an end-to-end flow without having to depend on third-party providers.

When deployed as part of the Docker deployment, the server is served on https://www.client.example behind the API gateway.